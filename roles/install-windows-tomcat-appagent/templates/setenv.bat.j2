@echo off
 
set CATALINA_OPTS=%CATALINA_OPTS% -Dallow.unsigned.sdk.extension.jars=true -javaagent:{{ binary_path }}\\javaagent.jar
 
set APPDYNAMICS_CONTROLLER_HOST_NAME="{{ controller_host }}"
set APPDYNAMICS_CONTROLLER_PORT="{{ controller_port }}"
set APPDYNAMICS_CONTROLLER_SSL_ENABLED="{{ ssl_enabled }}"
set APPDYNAMICS_AGENT_APPLICATION_NAME="{{ app_name }}"
set APPDYNAMICS_AGENT_UNIQUE_HOST_ID="{{ ansible_hostname }}"
set APPDYNAMICS_AGENT_TIER_NAME="{{ tier_name }}"
set APPDYNAMICS_AGENT_NODE_NAME="{{ node_name }}"
set APPDYNAMICS_AGENT_ACCOUNT_ACCESS_KEY="{{ access_key }}"